/** The countdown is set loose rather than tight, so the digits have air between
 *  them and the colon has something to sit in. */
export const CLOCK_TRACKING = '0.02em';

/** "FINISHED" is a longer word than any countdown, so it gets its own size. */
export const CLOCK_SIZE = 13;
export const FINISHED_CLOCK_SIZE = 6;

/** How many stat rows the left rail is ruled for. */
export const STAT_ROWS = 6;

/** The blind figures are wide, so the row between them is tight. */
export const BLIND_GAP = 2.2;

/** The payout table's own column heading, which ledger has no counterpart for —
 *  matched to the prize-pool label rather than left smaller than the rows. */
export const PAYOUT_HEADER_SIZE = 1.5;
export const PAYOUT_ROW_SIZE = 2;
