/** The countdown is set loose rather than tight, so the digits have air between
 *  them and the colon has something to sit in. */
export const CLOCK_TRACKING = '0.02em';

/** "FINISHED" is a longer word than any countdown, so it gets its own size. */
export const CLOCK_SIZE = 13;
export const FINISHED_CLOCK_SIZE = 6;

/**
 * The type scale, in projector units. Panel and Dial are held to these too —
 * choosing a layout is meant to change the arrangement, not how big the room
 * has to read.
 */
export const TITLE_SIZE = 4;
export const PRICE_LINE_SIZE = 2;
export const LEVEL_LABEL_SIZE = 3;
export const CHIP_RACE_SIZE = 1.8;
export const BLIND_LABEL_SIZE = 2;
export const BLIND_VALUE_SIZE = 4.4;
export const ANTE_UNIT_SIZE = 1.6;
export const NEXT_SIZE = 2.5;
export const STAT_LABEL_SIZE = 1.7;
export const STAT_VALUE_SIZE = 2.7;
export const PRIZE_LABEL_SIZE = 1.5;
export const PRIZE_VALUE_SIZE = 4.2;
export const PAYOUT_ROW_SIZE = 2;
