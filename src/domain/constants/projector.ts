/** Under a minute left on a play level — the clock, rail and dial all go coral. */
export const LOW_TIME_SECONDS = 60;

/** How many places carry a medal pip on the payout list. */
export const MEDAL_PLACES = 3;

/** Separator between the buy-in, re-buy and stack figures on the price line. */
export const PRICE_LINE_SEPARATOR = '   ·   ';
